<UserControl x:Class="HorseBarn.WPF.Views.HorseBarnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HorseBarn.WPF.Views"
             xmlns:lib="clr-namespace:HorseBarn.lib.Horse;assembly=HorseBarn.lib"
             xmlns:conv="clr-namespace:HorseBarn.WPF.Converters"
        mc:Ignorable="d" 
             Height="450" Width="800">
    <UserControl.Resources>
        <conv:IsValidToRedConverter x:Key="IsValidToRedConverter" />

    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Carts" />
        <Label Grid.Column="1" Grid.Row="0" Content="Horses" />

        <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding HorseBarn.Carts, NotifyOnSourceUpdated=True}" SelectionMode="Extended"></ListBox>

        <ListBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding HorseBarn.Pasture.Horses, NotifyOnSourceUpdated=True}" SelectionMode="Extended" Background="Blue">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Background="{Binding Path=IsValid, Converter={StaticResource IsValidToRedConverter}}" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" Content="Light Horse" />
                        <TextBox Grid.Row="2" Text="{Binding Path=Name, Mode=TwoWay}" Background="Green"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button x:Name="AddHorse">Add Horse</Button>
        </StackPanel>
    </Grid>
</UserControl>
